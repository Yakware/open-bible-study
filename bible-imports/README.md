# Instructions

Do not modify the files in src/lib/db. These are generated using the script `db:pull` automatically. The reason I do it this way is so that we only have to maintain one source DB schema, which is handled in the Wordway app folder.
